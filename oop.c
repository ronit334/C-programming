#include<stdio.h>
int main()
{
    int a[10],i,k[10],e=0;
    char s[10];
    printf("enter");
    gets(s);
    for(i=0;i<s[i];i++)
        a[i]=s[i];
    for(i=0;i<=s[i];i++)
    {
        if(a[i]!=105)
            s[e]=a[i];
        e++;
    }

        printf("%s",s);

}
